<?php
    require_once "index.php";

    if ($_POST) {
        $info = $_POST['info'];

        if (login exists) {
                if password is correct
                echo 'link "search-page": Login';
            }
        else {
                echo 'Create a new account';
            }
    $connect->close();
    }
?>